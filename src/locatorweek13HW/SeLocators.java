package locatorweek13HW;

public class SeLocators {

    // locator
    //1. by ID
    //id ="Name of Id "
    //driver.findElement(By.id("password");
    //2. by Name
   // Name = "Name of name "
    // driver.findElement(By.name("password")
    // 3.by xpath "//(tagName[@attribute='value']"
    // driver.findElement(By.xpath("//input[@name='Password']))
    //*[@attribute='value')]
    // driver.findElement(By.xpath(//*[@name='password']))
    //*[contains(@attribute,'value')]
    //driver.findElements(By.xpath(//*[contains(@name,'password')]
    //4.By CSS selector (tag#id)
    // driver.findElements(By.css(input#password)
    //tag[attribute=value)
    //driver.findElements(By.css("input[name='Password']))
    //tag.class
    //driver.findElement(By.css(input.className)
    //tag.class[attribute=value]
    // driver.findElement(By.css(input.class[class=password]

}

